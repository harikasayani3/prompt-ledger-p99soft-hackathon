// Supabase server client is no longer used — all data access goes through the
// MCP server at https://expense-remote-mcp-server.onrender.com/mcp
// Kept as a stub to avoid breaking any residual imports.
export const supabaseAdmin = {} as const;
